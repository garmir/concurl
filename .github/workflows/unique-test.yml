name: Unique Test - concurl
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - concurl
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: concurl ===" > unique-test-concurl.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-concurl.txt
            echo "Test ID: TEST_156" >> unique-test-concurl.txt
            echo "Timestamp: $(date)" >> unique-test-concurl.txt
            curl -s ifconfig.me >> unique-test-concurl.txt
            echo "Unique instruction: General framework compliance and performance testing" >> unique-test-concurl.txt
          '

      - name: Ubuntu Fallback - concurl
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for concurl" > fallback-concurl.txt
          curl -s ifconfig.me >> fallback-concurl.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-concurl
          path: "*.txt"
          retention-days: 2